# PAS.Policies.Client

An autogenerated C# API Client for consuming **PAS.Policies.Api**, built using [Microsoft Kiota](https://learn.microsoft.com/en-us/openapi/kiota/).

## Prerequisites

Before regenerating or updating the client code, ensure you have the Kiota CLI installed globally:
```
dotnet tool install -g Microsoft.OpenApi.Kiota
```

## First generation

To regenerate the client for the first time:
- Run the PAS.Policies API locally.
- Open a PowerShell prompt in the `src/Policies/Client` folder.
- Execute the following command if the api is currently running:
``` 
kiota generate -l cSharp -c PoliciesApiClient -n PAS.Policies.Client -d https://localhost:7116/openapi/v1.json -o .
```
- Or execute the following command if api was compiled (and openapi.json was generated):
``` 
kiota generate -l cSharp -c PoliciesApiClient -n PAS.Policies.Client -d ../Api/openapi.json -o .
```

## Regenerating

To regenerate the client:
- Run the PAS.Policies API locally.
- Open a PowerShell prompt in the `src/Policies/Client` folder.
- Execute the following command:
```
kiota update -o .
```